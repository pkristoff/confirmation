<div>
  <h2><%= t('views.common.status') %> <%= "#{current_candidate.account_name}" %></h2>

  <%= render('candidates/shared/event', formats: [:html], candidate: current_candidate, by_candidate: true) %>

</div>
