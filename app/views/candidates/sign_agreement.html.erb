<% require 'constants' %>

<%= render 'candidates/shared/two_column_signed_event',
           path: sign_agreement_update_path(@candidate.id),
           event_association: @candidate,
           shared_html_file: 'candidate_covenant_agreement',
           candidate_event: @candidate.get_candidate_event(Candidate.covenant_agreement_event_key),
           download_doc: download_document_path(@candidate.id, Event::Document::CANDIDATE_COVENANT),
           is_dev: false,
           is_verify: false
%>
