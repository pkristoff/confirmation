<%= form_tag monthly_mass_mailing_update_path, method: :put do %>

    <%= submit_tag t('email.mail'), id: 'top-update' %>

    <%= render partial: 'shared/reminder_body',
               :locals => {
                   render_mail_text: false
               } %>

    <%= render partial: 'shared/sorting_candidate_selection',
               :locals => {
                   candidates: @candidates,
                   route: :monthly_mass_mailing,
                   selected_candidate_ids: @selected_candidate_ids
               } %>

    <%= submit_tag t('email.mail'), id: 'bottom-update' %>

<% end %>