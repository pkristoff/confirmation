<%= require 'constants' %>

<%= render partial: 'candidates/shared/two_column_signed_event',
           :locals => {path: sponsor_agreement_verify_update_path(@candidate.id),
                       event_association: @candidate,
                       shared_html_file: 'sponsor_agreement',
                       candidate_event: @candidate.get_candidate_event(I18n.t('events.sponsor_agreement')),
                       download_doc: download_document_path(@candidate.id, Event::Document::CONVERSATION_SPONSOR_CANDIDATE),
                       is_dev: false
           } %>
